class Result
{
    public static int hurdleRace(int k, List<int> height)
    {    
        int numPotions = height.Max() - k;

        if (numPotions < 0)
        {
            return 0;
        }

        return numPotions;
    }
}